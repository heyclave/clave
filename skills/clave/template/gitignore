# build output
dist/
.astro/
.wrangler/

# dependencies
node_modules/

# QA captures (transient, not part of the record)
.qa/

# Clave's working dir is committed BY DEFAULT — clave.json (version ledger) and
# skeletons/ (design archive) are part of the record. Only the scratch subfolder is
# transient/local; ignore that one folder, commit everything else under .clave/.
.clave/scratch/

# environment
.env
.env.production
.dev.vars

# macOS
.DS_Store
